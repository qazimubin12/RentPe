@model RentPe.ViewModels.CustomOfferActionViewModel

@if (Model != null)
{
    <div class="modal-content">
        <div class="modal-heCustomOfferer">
            <h5 class="modal-title">
                <span>Delete Custom Offer</span>
            </h5>
            <button type="button" class="btn-close" data-dismiss="modal" aria-label="Close"></button>

        </div>
        <div class="modal-body">
            <form id="actionForm">
                <input type="hidden" name="ID" value="@Model.ID" />
            </form>
            <div class="alert alert-danger" role="alert">
                Are you sure you want to delete this Custom Offer?
            </div>
        </div>
        <div class="modal-footer">
            <button id="actionButton" type="button" class="btn btn-danger"><i class="fas fa-trash-alt mr-1"></i> Delete</button>
            <button type="button" class="btn btn-secondary" data-dismiss="modal"><i class="fas fa-times mr-1"></i> Close</button>
        </div>
    </div>
}

<script>
	$("#actionButton").click(function () {
		$.ajax({
			url: '@Url.Action("Delete", "CustomOffer")',
			type: "post",
			data: $("#actionForm").serialize()
		})
		.done(function (response) {
            if (response.success)
			{
				window.location.href = "@Url.Action("Index", "CustomOffer")";
			}
			else {
				$(".errorDiv").html(response.Message);
			}
		});
	});
</script>